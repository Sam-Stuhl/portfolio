{
  "name": "portfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "node build/fetch-resume.mjs",
    "render": "node build/render.mjs",
    "build": "npm run fetch && npm run render",
    "check": "tsc --noEmit",
    "dev": "npm run build && wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.7.2",
    "wrangler": "^4.129.0"
  }
}
